# Copy this file to .env.local (automatic on `npm run dev` if .env.cloud exists).
# NEVER commit real tokens.

NEXT_PUBLIC_STRAPI_URL=https://your-project.strapiapp.com
STRAPI_API_TOKEN=paste-read-only-token-here
